{
  "name": "benches",
  "private": true,
  "description": "commitlint, for the latency head-to-head",
  "devDependencies": {
    "@commitlint/cli": "^21",
    "@commitlint/config-conventional": "^21"
  }
}
